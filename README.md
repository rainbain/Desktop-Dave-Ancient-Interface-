# Desktop Dave
This is the source code to the anchient interface.

Very quickly made, (in about an hour), so a good portion of the CSS and JS is AI generated, so my applogies.
Anyhow, its quite useful.